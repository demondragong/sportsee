/**
 * Format the activity data returned by the sportsee API for use in the SessionLengthLineChart component
 * @param {Object} data Data returned by the API
 */
 export default function formatAverageSessionData(data) {
  return data
}
